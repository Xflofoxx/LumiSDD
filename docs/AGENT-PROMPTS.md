# LumiSDD Agent Prompts

Standardized prompts for AI agents working on LumiSDD projects.

---

## 1. Code-Writer Agent

**Purpose:** Generate implementation code from specification.

```markdown
## Task
Implement the requirement REQ-XXX from spec.yaml.

## Specification
- Spec file: examples/user-crud/spec.yaml
- Requirement ID: REQ-XXX
- Requirement: <description from spec>
- Priority: <high|medium|low>

## Constraints
- Use TypeScript with strict typing
- Follow existing code patterns in src/generated/
- Include reqId comments: // req: REQ-XXX
- Include constraint comments: // req: CON-XXX
- All functions must be exported
- Use in-memory storage (Map) for data persistence

## Output
Generate a git patch:
```bash
git diff --no-color > feature/req-xxx.patch
```

## Example Comment Pattern
// req: REQ-UC-001
// req: CON-UC-002
```

---

## 2. Reviewer Agent

**Purpose:** Review code implementation for compliance and quality.

```markdown
## Task
Review implementation for REQ-XXX requirement.

## Specification
- File to review: src/generated/user-service.ts
- Requirement ID: REQ-XXX
- Requirement: <description>
- Constraints to verify:
  - CON-XXX: <constraint description>

## Review Checklist
- [ ] reqId comment present
- [ ] All constraint comments present
- [ ] TypeScript strict mode compliance
- [ ] Error handling implemented
- [ ] Edge cases handled
- [ ] No hardcoded values

## Output
Generate a git patch with fixes:
```bash
git diff --no-color > review/req-xxx-fix.patch
```

## Review Report Format
| Check | Status | Notes |
|-------|--------|-------|
| reqId comment | PASS/FAIL | - |
| Constraint compliance | PASS/FAIL | - |
| Type safety | PASS/FAIL | - |
| Error handling | PASS/FAIL | - |
```

---

## 3. Tester Agent

**Purpose:** Generate unit tests for implemented requirements.

```markdown
## Task
Write unit tests for REQ-XXX requirement.

## Specification
- Requirement ID: REQ-XXX
- Implementation file: src/generated/user-service.ts
- Test file: test/user-service.test.ts

## Requirements
- Test each acceptance criteria
- Include reqId comment in test describe block
- Use Vitest framework
- Mock external dependencies if needed
- Ensure 80%+ code coverage

## Test Template
```typescript
describe('REQ-XXX: <requirement>', () => {
  it('should <acceptance criteria 1>', () => {
    // req: REQ-XXX
  });
  
  it('should <acceptance criteria 2>', () => {
    // req: REQ-XXX
  });
});
```

## Output
Generate test patch:
```bash
git diff --no-color > tests/req-xxx.patch
```

## Run Tests
```bash
npm test -- <test-file>
```
```

---

## 4. Combined Workflow Agent

**Purpose:** Execute full SDD workflow (write + review + test).

```markdown
## Task
Complete SDD workflow for REQ-XXX.

## Workflow
1. **Write:** Implement requirement from spec.yaml
2. **Review:** Verify implementation against constraints
3. **Test:** Generate and run unit tests

## Specification
- Spec file: examples/user-crud/spec.yaml
- Requirement ID: REQ-XXX

## Constraints
- All code must compile: `npm run typecheck`
- All tests must pass: `npm test`
- Lint warnings only (no errors): `npm run lint`
- Coverage > 80%

## Output
Generate complete patch:
```bash
git diff --no-color > workflow/req-xxx-complete.patch
```

## Verification Commands
```bash
npm run typecheck  # Must pass
npm test           # All tests pass
npm run lint       # No errors
```
```

---

## 5. Documentation Agent

**Purpose:** Generate and update documentation.

```markdown
## Task
Update traceability matrix for implemented requirements.

## Specification
- Requirements: List of REQ-XXX, CON-XXX
- Source files: src/generated/*.ts
- Test files: test/*.test.ts

## Output
Update traceability-matrix.md:
```bash
./update-traceability.sh
```

## Documentation Requirements
- Include requirement ID
- Include implementation location (file:line)
- Include test status (PASS/FAIL)
- Include constraint verification
```

---

## Usage Example

```bash
# Full workflow
git checkout -b feature/req-001

# Generate code
cat > /tmp/writer-prompt.md << 'EOF'
Task: Implement REQ-UC-001 from spec.yaml
...
EOF

# Apply changes
git add -A
git diff --no-color > changes.patch

# Review
git apply changes.patch
npm run typecheck
npm test

# Create PR
git checkout main
git merge feature/req-001
git push origin main
```

---

*Auto-generated by LumiSDD*
